name: types
description: >
  The types of suggestion to return. Multiple types can be passed using the pipe character (`|`) as a separator.
  For a complete list of types supported, please see [All Types](#/schemas/LocalitiesTypesAll)
schema:
  default:
    locality|postal_code
  example: locality|airport|admin_level
  $ref: "../../schemas/LocalitiesTypesAll.yml"
  writeOnly: true
#  anyOf:
#    - $ref: "../../schemas/LocalitiesTypes.yml"
#    - $ref: "../../schemas/LocalitiesTypesPoi.yml"


in: query
